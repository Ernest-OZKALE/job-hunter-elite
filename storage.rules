rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files stored under users/{userId}/files/... are only accessible by that user
    match /users/{userId}/files/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
